pre-commit:
  jobs:
    - name: checks
      run: bun scripts/precommit.mjs

pre-push:
  jobs:
    - name: react-doctor
      files: git ls-files
      # Keep the placeholder so Lefthook runs the full scan on every push.
      run: 'bun run doctor # {files}'
